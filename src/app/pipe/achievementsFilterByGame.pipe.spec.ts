/* tslint:disable:no-unused-variable */

import { TestBed, async } from '@angular/core/testing';
import { AchievementsFilterByGamePipe } from './achievementsFilterByGame.pipe';

describe('Pipe: AchievementsFilterByGamee', () => {
  it('create an instance', () => {
    let pipe = new AchievementsFilterByGamePipe();
    expect(pipe).toBeTruthy();
  });
});
