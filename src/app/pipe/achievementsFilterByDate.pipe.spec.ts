/* tslint:disable:no-unused-variable */

import { TestBed, async } from '@angular/core/testing';
import { AchievementsFilterByDatePipe } from './achievementsFilterByDate.pipe';

describe('Pipe: AchievementsFilterByDatee', () => {
  it('create an instance', () => {
    let pipe = new AchievementsFilterByDatePipe();
    expect(pipe).toBeTruthy();
  });
});
