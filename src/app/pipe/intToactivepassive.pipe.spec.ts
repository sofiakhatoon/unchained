/* tslint:disable:no-unused-variable */

import { TestBed, async } from '@angular/core/testing';
import { IntToactivepassivePipe } from './intToactivepassive.pipe';

describe('Pipe: IntToactivepassivee', () => {
  it('create an instance', () => {
    let pipe = new IntToactivepassivePipe();
    expect(pipe).toBeTruthy();
  });
});
