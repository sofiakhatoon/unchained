/* tslint:disable:no-unused-variable */

import { TestBed, async } from '@angular/core/testing';
import { AchievementsSearchPipe } from './AchievementsSearch.pipe';

describe('Pipe: AchievementsSearche', () => {
  it('create an instance', () => {
    let pipe = new AchievementsSearchPipe();
    expect(pipe).toBeTruthy();
  });
});
